# API

Next.js Route Handlers live under `app/api/*`.

This directory exists to match the high-level repo architecture (`/api`, `/app`, `/lib`, `/db`, `/components`) and can be used for non-Next API code if needed later.

